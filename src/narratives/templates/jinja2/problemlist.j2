{% from "utility-fragments.j2" import codeableConcept, concat, renderTime, narrative_link_id, safe_concat %}

{# Problem List #}
<h5>Problem List</h5>
<table class="hapiPropertyTable">
  <thead>
    <tr>
      <th>Medical Problems</th>
      <th>Status</th>
      <th>Comments</th>
      <th>Onset Date</th>
    </tr>
  </thead>
  <tbody>
    {% for entry in resource.entry %}
      {% set cond = entry.resource %}
      {% if cond.resourceType != 'Composition' %}
        {% if cond.extension is defined and cond.extension %}
          {% set extension = cond.extension | selectattr('url', 'equalto', 'http://hl7.org/fhir/StructureDefinition/narrativeLink') | list | first %}
        {% else %}
          {% set extension = None %}
        {% endif %}
        <tr id="{{ narrative_link_id(extension) }}">
          <td>{{ codeableConcept(cond.code) }}</td>
          <td>{{ codeableConcept(cond.clinicalStatus) }}</td>
          <td>{{ safe_concat(cond.note, 'text') }}</td>
          <td>{{ renderTime(cond.onset) }}</td>
        </tr>
      {% endif %}
    {% endfor %}
  </tbody>
</table>
